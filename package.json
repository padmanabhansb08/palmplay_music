{
  "name": "palmplay-music",
  "private": true,
  "scripts": {
    "config": "node scripts/generate-config.js",
    "build": "node scripts/generate-config.js && npx terser pamplay-frontend/app.js -o pamplay-frontend/app.js -c -m && npx clean-css-cli -o pamplay-frontend/style.css pamplay-frontend/style.css",
    "prestart": "node scripts/generate-config.js",
    "start": "npx http-server -p 8000"
  },
  "dependencies": {
    "puppeteer": "^25.3.0"
  }
}
